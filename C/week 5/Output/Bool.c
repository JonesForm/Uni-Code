#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool b = false;
    printf("Bool is set to false with number: %d\n", b);
    b = true;
    printf("Now the bool is set to true with number: %d\n", b);
    return 0;
}